-- =====================================================================
-- 013: 도중유국 선언자 기록 (구종구패 카운트용)
--   abortion_player: 도중유국을 선언한 사람 (주로 구종구패)
--   옛 데이터는 NULL (선언자 정보 없음 → 카운트 불가, 신규 기록부터 집계)
-- =====================================================================

ALTER TABLE hand_results
  ADD COLUMN IF NOT EXISTS abortion_player VARCHAR(50);
